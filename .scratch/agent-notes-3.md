changed:
- created `test/fixtures/gemini.ts` and began extracting Gemini fixtures plus plain shape assertions.
- created empty `e2e/gemini/` and `test/fixtures/` directories as part of the interrupted implementation.

status:
- stopped immediately on user instruction.
- no further commands or verification were run after the stop request.
- lead is replacing the out-of-scope generic e2e/anthropic/common shape and generated artifacts.
